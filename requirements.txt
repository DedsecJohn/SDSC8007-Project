# ============================================================
# Feedback Prize - English Language Learning
# Python 依赖清单 (Stage 2 Baseline)
# ============================================================
# 建议 Python 版本: 3.10 或 3.11
# 安装命令:
#     pip install -r requirements.txt
#
# 如需 CUDA 版 PyTorch (Linux + NVIDIA GPU)：
#     pip install torch --index-url https://download.pytorch.org/whl/cu121
#     pip install -r requirements.txt
# ============================================================

# ---------- 深度学习核心 ----------
torch>=2.0.0,<2.6.0
transformers>=4.35.0,<5.0.0
tokenizers>=0.14.0
sentencepiece>=0.1.99          # DeBERTa-v3 tokenizer 必需
protobuf>=3.20.0,<5.0.0        # sentencepiece / HF 依赖
accelerate>=0.24.0             # transformers 训练工具集
safetensors>=0.4.0             # HF 权重格式

# ---------- 数据科学基础 ----------
numpy>=1.23.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0            # StratifiedKFold / metrics
scipy>=1.10.0

# ---------- 工具类 ----------
tqdm>=4.65.0                   # 进度条
pyyaml>=6.0                    # 配置 (可选)

# ---------- 开发/调试 (可选但推荐) ----------
jupyter>=1.0.0
ipykernel>=6.25.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ---------- Kaggle 环境一般已预装，无需安装，仅本地使用 ----------
# 如本地需要上传/下载 Kaggle 数据，可取消注释：
# kaggle>=1.5.16
